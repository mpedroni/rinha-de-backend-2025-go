services:
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api1
      - api2
    networks:
      - api-network
      - payment-processor
  api1: &api
    image: mpedroni/rinha-backend-2025:latest
    hostname: api1
    environment:
      - SERVICE_NAME=api-1
      - PORT=3000
      - DEBUG=true
      - PROCESSOR_DEFAULT_URL=http://payment-processor-default:8080
      - PROCESSOR_FALLBACK_URL=http://payment-processor-fallback:8080
    networks:
      - api-network
      - payment-processor

  api2:
    <<: *api
    hostname: api2
    environment:
      - SERVICE_NAME=api-2
      - PORT=3000
      - DEBUG=true
      - PROCESSOR_DEFAULT_URL=http://payment-processor-default:8080
      - PROCESSOR_FALLBACK_URL=http://payment-processor-fallback:8080
    networks:
      - api-network
      - payment-processor

networks:
  api-network:
    driver: bridge
  payment-processor:
    external: true
